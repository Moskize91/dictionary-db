name: netless-dictionary-db
path:
  dist: ./dist
  saveTo: ./node_modules

scripts:
  didSetup: if test -d ./node_modules; then echo "true"; else echo "false"; fi
  setup: yarn install --frozen-lockfile
  check: yarn run check
  buildDev: yarn run build
  buildProd: yarn run prod